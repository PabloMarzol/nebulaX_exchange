import type { OmitFirst, OverloadedParameters } from "../_base.ts";
import type { ISubscriptionTransport } from "../../transport/base.ts";
import type { SubscriptionRequestConfig } from "./_types.ts";
import { activeAssetCtx } from "./activeAssetCtx.ts";
import { activeAssetData } from "./activeAssetData.ts";
import { activeSpotAssetCtx } from "./activeSpotAssetCtx.ts";
import { allMids } from "./allMids.ts";
import { assetCtxs } from "./assetCtxs.ts";
import { bbo } from "./bbo.ts";
import { candle } from "./candle.ts";
import { clearinghouseState } from "./clearinghouseState.ts";
import { explorerBlock } from "./explorerBlock.ts";
import { explorerTxs } from "./explorerTxs.ts";
import { l2Book } from "./l2Book.ts";
import { notification } from "./notification.ts";
import { openOrders } from "./openOrders.ts";
import { orderUpdates } from "./orderUpdates.ts";
import { spotState } from "./spotState.ts";
import { trades } from "./trades.ts";
import { userEvents } from "./userEvents.ts";
import { userFills } from "./userFills.ts";
import { userFundings } from "./userFundings.ts";
import { userHistoricalOrders } from "./userHistoricalOrders.ts";
import { userNonFundingLedgerUpdates } from "./userNonFundingLedgerUpdates.ts";
import { userTwapHistory } from "./userTwapHistory.ts";
import { userTwapSliceFills } from "./userTwapSliceFills.ts";
import { webData2 } from "./webData2.ts";
import { webData3 } from "./webData3.ts";
export type { ActiveAssetCtxEvent as WsActiveAssetCtxEvent, ActiveAssetCtxParameters as WsActiveAssetCtxParameters, } from "./activeAssetCtx.ts";
export type { ActiveAssetDataEvent as WsActiveAssetDataEvent, ActiveAssetDataParameters as WsActiveAssetDataParameters, } from "./activeAssetData.ts";
export type { ActiveSpotAssetCtxEvent as WsActiveSpotAssetCtxEvent, ActiveSpotAssetCtxParameters as WsActiveSpotAssetCtxParameters, } from "./activeSpotAssetCtx.ts";
export type { AllMidsEvent as WsAllMidsEvent, AllMidsParameters as WsAllMidsParameters } from "./allMids.ts";
export type { AssetCtxsEvent as WsAssetCtxsEvent, AssetCtxsParameters as WsAssetCtxsParameters } from "./assetCtxs.ts";
export type { BboEvent as WsBboEvent, BboParameters as WsBboParameters } from "./bbo.ts";
export type { CandleEvent as WsCandleEvent, CandleParameters as WsCandleParameters } from "./candle.ts";
export type { ClearinghouseStateEvent as WsClearinghouseStateEvent, ClearinghouseStateParameters as WsClearinghouseStateParameters, } from "./clearinghouseState.ts";
export type { ExplorerBlockEvent as WsExplorerBlockEvent, ExplorerBlockParameters as WsExplorerBlockParameters, } from "./explorerBlock.ts";
export type { ExplorerTxsEvent as WsExplorerTxsEvent, ExplorerTxsParameters as WsExplorerTxsParameters, } from "./explorerTxs.ts";
export type { L2BookEvent as WsL2BookEvent, L2BookParameters as WsL2BookParameters } from "./l2Book.ts";
export type { NotificationEvent as WsNotificationEvent, NotificationParameters as WsNotificationParameters, } from "./notification.ts";
export type { OpenOrdersEvent as WsOpenOrdersEvent, OpenOrdersParameters as WsOpenOrdersParameters, } from "./openOrders.ts";
export type { OrderUpdatesEvent as WsOrderUpdatesEvent, OrderUpdatesParameters as WsOrderUpdatesParameters, } from "./orderUpdates.ts";
export type { SpotStateEvent as WsSpotStateEvent, SpotStateParameters as WsSpotStateParameters } from "./spotState.ts";
export type { TradesEvent as WsTradesEvent, TradesParameters as WsTradesParameters } from "./trades.ts";
export type { UserEventsEvent as WsUserEventsEvent, UserEventsParameters as WsUserEventsParameters, } from "./userEvents.ts";
export type { UserFillsEvent as WsUserFillsEvent, UserFillsParameters as WsUserFillsParameters } from "./userFills.ts";
export type { UserFundingsEvent as WsUserFundingsEvent, UserFundingsParameters as WsUserFundingsParameters, } from "./userFundings.ts";
export type { UserHistoricalOrdersEvent as WsUserHistoricalOrdersEvent, UserHistoricalOrdersParameters as WsUserHistoricalOrdersParameters, } from "./userHistoricalOrders.ts";
export type { UserNonFundingLedgerUpdatesEvent as WsUserNonFundingLedgerUpdatesEvent, UserNonFundingLedgerUpdatesParameters as WsUserNonFundingLedgerUpdatesParameters, } from "./userNonFundingLedgerUpdates.ts";
export type { UserTwapHistoryEvent as WsUserTwapHistoryEvent, UserTwapHistoryParameters as WsUserTwapHistoryParameters, } from "./userTwapHistory.ts";
export type { UserTwapSliceFillsEvent as WsUserTwapSliceFillsEvent, UserTwapSliceFillsParameters as WsUserTwapSliceFillsParameters, } from "./userTwapSliceFills.ts";
export type { WebData2Event as WsWebData2Event, WebData2Parameters as WsWebData2Parameters } from "./webData2.ts";
export type { WebData3Event as WsWebData3Event, WebData3Parameters as WsWebData3Parameters } from "./webData3.ts";
export type { SubscriptionRequestConfig } from "./_types.ts";
/**
 * A client for interacting with the Hyperliquid Subscription API.
 * @typeParam T - The transport (extends {@linkcode ISubscriptionTransport}) used to connect to the Hyperliquid API.
 */
export declare class SubscriptionClient<T extends ISubscriptionTransport = ISubscriptionTransport> implements SubscriptionRequestConfig<T> {
    transport: T;
    /**
     * Initialises a new instance.
     * @param args - The arguments for initialisation.
     *
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     * const subsClient = new hl.SubscriptionClient({ transport });
     * ```
     */
    constructor(args: SubscriptionRequestConfig<T>);
    /**
     * Subscribe to context updates for a specific perpetual asset.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.activeAssetCtx({ coin: "ETH" }, (data) => console.log(data));
     * ```
     */
    activeAssetCtx(...args: OmitFirst<OverloadedParameters<typeof activeAssetCtx>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to trading data updates for a specific asset and user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.activeAssetData({ coin: "ETH", user: "0x..." }, (data) => console.log(data));
     * ```
     */
    activeAssetData(...args: OmitFirst<OverloadedParameters<typeof activeAssetData>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to context updates for a specific spot asset.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.activeSpotAssetCtx({ coin: "@1" }, (data) => console.log(data));
     * ```
     */
    activeSpotAssetCtx(...args: OmitFirst<OverloadedParameters<typeof activeSpotAssetCtx>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to mid prices for all actively traded assets.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.allMids((data) => console.log(data));
     * ```
     */
    allMids(...args: OmitFirst<OverloadedParameters<typeof allMids>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to asset contexts for all perpetual assets.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see null
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.assetCtxs((data) => console.log(data));
     * ```
     */
    assetCtxs(...args: OmitFirst<OverloadedParameters<typeof assetCtxs>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to best bid and offer updates for a specific asset.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.bbo({ coin: "ETH" }, (data) => console.log(data));
     * ```
     */
    bbo(...args: OmitFirst<OverloadedParameters<typeof bbo>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to candlestick data updates for a specific asset.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.candle({ coin: "ETH", interval: "1h" }, (data) => console.log(data));
     * ```
     */
    candle(...args: OmitFirst<OverloadedParameters<typeof candle>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to clearinghouse state updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see null
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.clearinghouseState({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    clearinghouseState(...args: OmitFirst<OverloadedParameters<typeof clearinghouseState>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to explorer block updates.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     * @note Make sure the endpoint in the {@link transport} supports this method.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see null
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport({ url: "wss://rpc.hyperliquid.xyz/ws" }); // RPC endpoint
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.explorerBlock((data) => console.log(data));
     * ```
     */
    explorerBlock(...args: OmitFirst<OverloadedParameters<typeof explorerBlock>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to explorer transaction updates.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     * @note Make sure the endpoint in the {@link transport} supports this method.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see null
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport({ url: "wss://rpc.hyperliquid.xyz/ws" }); // RPC endpoint
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.explorerTxs((data) => console.log(data));
     * ```
     */
    explorerTxs(...args: OmitFirst<OverloadedParameters<typeof explorerTxs>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to L2 order book updates for a specific asset.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.l2Book({ coin: "ETH" }, (data) => console.log(data));
     * ```
     */
    l2Book(...args: OmitFirst<OverloadedParameters<typeof l2Book>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to notification updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.notification({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    notification(...args: OmitFirst<OverloadedParameters<typeof notification>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to open orders updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.openOrders({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    openOrders(...args: OmitFirst<OverloadedParameters<typeof openOrders>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to order status updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.orderUpdates({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    orderUpdates(...args: OmitFirst<OverloadedParameters<typeof orderUpdates>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to spot state updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.spotState({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    spotState(...args: OmitFirst<OverloadedParameters<typeof spotState>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to real-time trade updates for a specific asset.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.trades({ coin: "ETH" }, (data) => console.log(data));
     * ```
     */
    trades(...args: OmitFirst<OverloadedParameters<typeof trades>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to non-order events for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     * @note Different subscriptions cannot be distinguished from each other.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.userEvents({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    userEvents(...args: OmitFirst<OverloadedParameters<typeof userEvents>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to trade fill updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.userFills({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    userFills(...args: OmitFirst<OverloadedParameters<typeof userFills>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to funding payment updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.userFundings({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    userFundings(...args: OmitFirst<OverloadedParameters<typeof userFundings>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to historical order updates for a specific user.
     * @param config - General configuration for Subscription API subscriptions.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.userHistoricalOrders({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    userHistoricalOrders(...args: OmitFirst<OverloadedParameters<typeof userHistoricalOrders>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to non-funding ledger updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.userNonFundingLedgerUpdates({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    userNonFundingLedgerUpdates(...args: OmitFirst<OverloadedParameters<typeof userNonFundingLedgerUpdates>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to TWAP order history updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.userTwapHistory({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    userTwapHistory(...args: OmitFirst<OverloadedParameters<typeof userTwapHistory>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to TWAP execution updates for a specific user.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.userTwapSliceFills({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    userTwapSliceFills(...args: OmitFirst<OverloadedParameters<typeof userTwapSliceFills>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to comprehensive user and market data updates.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.webData2({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    webData2(...args: OmitFirst<OverloadedParameters<typeof webData2>>): Promise<import("../../transport/base.ts").Subscription>;
    /**
     * Subscribe to comprehensive user and market data updates.
     * @param params - Parameters specific to the API subscription.
     * @param listener - A callback function to be called when the event is received.
     * @returns A request-promise that resolves with a {@link Subscription} object to manage the subscription lifecycle.
     *
     * @throws {TransportError} When the transport layer throws an error.
     *
     * @see https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/websocket/subscriptions
     * @example
     * ```ts
     * import * as hl from "@nktkas/hyperliquid";
     *
     * const transport = new hl.WebSocketTransport();
     *
     * const client = new hl.SubscriptionClient({ transport });
     * const sub = await client.webData3({ user: "0x..." }, (data) => console.log(data));
     * ```
     */
    webData3(...args: OmitFirst<OverloadedParameters<typeof webData3>>): Promise<import("../../transport/base.ts").Subscription>;
}
//# sourceMappingURL=~client.d.ts.map