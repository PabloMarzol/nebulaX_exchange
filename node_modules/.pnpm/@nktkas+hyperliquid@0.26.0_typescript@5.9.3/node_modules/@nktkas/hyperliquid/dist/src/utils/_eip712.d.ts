export interface Types {
    [type: string]: {
        name: string;
        type: string;
    }[];
}
export interface Domain {
    name?: string;
    version?: string;
    chainId?: number | string | bigint | `0x${string}`;
    verifyingContract?: `0x${string}`;
    salt?: `0x${string}`;
}
/** Signs typed data with a private key. */
export declare function signTypedData(args: {
    privateKey: string | Uint8Array;
    domain?: Domain;
    types: Types;
    primaryType: string;
    message: Record<string, unknown>;
}): Promise<`0x${string}`>;
/** Hashes typed data according to EIP-712. */
export declare function hashTypedData(args: {
    domain: Domain;
    types: Types;
    primaryType: string;
    message: Record<string, unknown>;
}): Uint8Array;
import type { AbstractViemLocalAccount } from "../signing/mod.ts";
/** A minimalist EIP-712 signer using a raw private key. */
export declare class PrivateKeyEIP712Signer implements AbstractViemLocalAccount {
    #private;
    address: `0x${string}`;
    constructor(privateKey: string | Uint8Array);
    signTypedData(params: {
        domain: Domain;
        types: Types;
        primaryType: string;
        message: Record<string, unknown>;
    }): Promise<`0x${string}`>;
}
//# sourceMappingURL=_eip712.d.ts.map